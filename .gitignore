# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# environment
.env
.env.local
.env.*.local

# python build artifacts
__pycache__/
*.pyc

# plugin-cache artifacts (Claude Code writes these during version bumps)
.orphaned_at

# local-only maintainer config (stays out of the public repo).
# NOTE: docs/ IS tracked and pushed as of 2026-07-01 (policy change) — it is NOT
# ignored. The pre-push hook now guards against secrets/credentials, not docs/.
CLAUDE.local.md
.claude/settings.json

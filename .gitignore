# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# environment
.env
.env.local
.env.*.local

# plugin-cache artifacts (Claude Code writes these during version bumps)
.orphaned_at

# local-only: never published to the public repo.
# NOTE: docs/ is intentionally NOT ignored — it stays visible/greppable on disk
# and is deleted before pushing to main. A local pre-push hook (.git/hooks/pre-push)
# blocks any push that contains docs/, so internal content can't leak to history.
CLAUDE.local.md
.claude/settings.json

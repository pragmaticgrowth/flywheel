# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# environment
.env
.env.local
.env.*.local

# plugin-cache artifacts (Claude Code writes these during version bumps)
.orphaned_at

# local-only: internal docs, local Claude instructions, per-user editor config.
# Kept on disk for the maintainer; never published to the public repo.
docs/
CLAUDE.local.md
.claude/settings.json

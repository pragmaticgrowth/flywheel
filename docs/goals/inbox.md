# Goal inbox — capture-only follow-ups

Converted to goals by /define-goal, which removes the line.

- [ ] 2026-08-27 field-analysis (romy claude drain) — mid-run death strands verified work: romy sits 8 commits ahead of origin with an orphaned in_progress claim (227) and no implementer alive; the session shows only 2 pushes across ~14 settled goals because the Ship step fires only at the terminal stop. Fix: push after each `chore(goals): complete` (per-settle, non-blocking), and Phase 0 releases an orphaned in_progress claim whose implementer is provably gone. (evidence: `git -C /root/romy status -sb` = ahead 8; session d4ca23e3…jsonl)

# Goal inbox — capture-only follow-ups

Converted to goals by /define-goal, which removes the line.

- [ ] 2026-08-20 nonresidenttax/process-inbox bug — the implementer report file `~/.local/state/pg-dispatch/<SLUG>/reports/<id>-report.md` is enforced by prose only: `skills/dispatch/references/implementer-brief.md:173-174,209` demands it ("a missing report file for non-trivial work is itself a gate finding") while `skills/dispatch/SKILL.md:918-922` says "absent is fine — the diff and goal file suffice", and `skills/dispatch/scripts/pg_validate.py` never checks the reports/ directory at all. Measured: nonresidenttax goals 140-148 (run under ≤11.8.0 this morning) all wrote reports; goals 149-155 (run 15:15-16:14 under 12.1.0, installed 11:51) wrote NONE, yet their captures cite "report §4/§6/§8" and goal 151's production VERIFY runbook lived only in the unwritten report. Fix: make Arm A (`pg_validate.py`) require the report file to exist, be non-empty, and have mtime after gate_base before `completed`; reconcile the SKILL.md:918-922 wording so the crash-recovery allowance does not read as a general one. (evidence: cited lines; `ls ~/.local/state/pg-dispatch/nonresidenttax/reports/`)
- [ ] 2026-08-27 field-analysis (4-day workflow, opus-verified) — dispatch's Ship step is singular ("RUN that path now", one `shipped:` outcome) but nonresidenttax has plural publish paths: staging→main deploys only the Vercel app while each Worker needs `pnpm --filter @nt/<app> deploy:production` — a promotion touching both lands split-brain yet reports shipped ok. Fix in SKILL.md:543-554: when the repo's own docs declare >1 publish path, run every declared path the diff touched, report per-service; one shipped + one not = `ship FAILED: partial (<service> unshipped)` under needs-you `environment failure`. Keys strictly off the repo's docs — dispatch still never invents a deploy. (evidence: nonresidenttax cutover session e84e0e5c…jsonl; risk-shaped, no dispatch fire has mis-reported yet)
- [ ] 2026-08-27 field-analysis — process-inbox v12 envelope contradiction: step 3 promises an unrunnable PRODUCTION-CHECK goes "to the report's needs-you list with the exact query spelled out" but step 7's hard envelope permits only counts + dispatch line + OWNER lines, so a compliant report can count P checks and print none of the queries. Fix step 7: one line per unrunnable check (query + why), `<P>` equals lines printed, mirroring the `<O>` equality rule; update the matching red flag. (evidence: skills/process-inbox/SKILL.md:122-125 vs :209-219 — text conflict, verified against current file)
- [ ] 2026-08-27 field-analysis — once report-file enforcement ships (line above, 2026-08-20 capture), scope dispatch SKILL.md:918-922's "absent is fine — the diff and goal file suffice" with "for a crash-recovered goal only" so the crash-recovery allowance can't be read against the enforcement rule. (evidence: opus verification confirmed 918-922 is correctly crash-scoped today; the clause is belt-and-braces, ship with the enforcement goal)
- [ ] 2026-08-27 field-analysis — parallel-lane teammate idle notifications pile up as stale pings: 307 `idle_notification` occurrences across two continuation sessions of one mfa drain; one turn opened with eight consecutive stale pings after the run finished, costing an explanation turn. Fix: the parallel-mode report/handoff step drains or dismisses pending teammate messages before the closing turn. (evidence: mfa sessions 446477cd, b30fcd80 — grep count; pure noise/token waste, no correctness impact)
- [ ] 2026-08-27 field-analysis (droid/mfa) — the implementer brief's Droid fresh-check path (`droid exec -f <prompt-file>`) doesn't mandate `--enabled-tools`: a real lens run came back "UNVERIFIABLE — no filesystem tools loaded" and only succeeded on retry as `droid exec --enabled-tools "Read,Grep,Glob,LS,Execute" -f <brief>`. Fix: bake the flag into the sanctioned droid-exec lens command wherever the skill text spells it out. (evidence: mfa droid sessions cf783e1f/d8a13d7f — failure then confirmed working retry)

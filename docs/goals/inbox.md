# Goal inbox — capture-only follow-ups

Converted to goals by /define-goal, which removes the line.

- [ ] 2026-08-20 nonresidenttax/process-inbox bug — the implementer report file `~/.local/state/pg-dispatch/<SLUG>/reports/<id>-report.md` is enforced by prose only: `skills/dispatch/references/implementer-brief.md:173-174,209` demands it ("a missing report file for non-trivial work is itself a gate finding") while `skills/dispatch/SKILL.md:918-922` says "absent is fine — the diff and goal file suffice", and `skills/dispatch/scripts/pg_validate.py` never checks the reports/ directory at all. Measured: nonresidenttax goals 140-148 (run under ≤11.8.0 this morning) all wrote reports; goals 149-155 (run 15:15-16:14 under 12.1.0, installed 11:51) wrote NONE, yet their captures cite "report §4/§6/§8" and goal 151's production VERIFY runbook lived only in the unwritten report. Fix: make Arm A (`pg_validate.py`) require the report file to exist, be non-empty, and have mtime after gate_base before `completed`; reconcile the SKILL.md:918-922 wording so the crash-recovery allowance does not read as a general one. (evidence: cited lines; `ls ~/.local/state/pg-dispatch/nonresidenttax/reports/`)

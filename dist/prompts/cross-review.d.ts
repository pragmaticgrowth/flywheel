/**
 * Prompt framing for cross-model review — wraps user prompt with
 * instructions optimized for parallel multi-model execution.
 */
export declare function buildCrossReviewPrompt(userPrompt: string): string;

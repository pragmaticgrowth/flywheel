/**
 * Intelligent prompt for do_adversarial_review — challenges the implementation
 * approach rather than just checking for defects.
 * Adapted from codex-plugin-cc adversarial-review prompt.
 */
export declare function buildAdversarialReviewPrompt(userPrompt: string): string;
